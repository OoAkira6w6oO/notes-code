# 基本定义

```python
def getRepeatedData(data, repeatCnt):
    return data * repeatCnt

# 调用函数
genData = getRepeatedData("*", 5)
print(genData)  # 输出：*****

# 可以指定参数类型和返回值类型
def getRepeatedData(data: str, repeatCnt: int) -> str:
    return data * repeatCnt
```

